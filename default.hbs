<!DOCTYPE html>
<html>
    <head>
        <!--[if lte IE 9]>
        <script type="text/javascript">
            window.location = "/browser-compatibility";
        </script>
        <![endif]-->
        <title>{{meta_title}}</title>
        {{ghost_head}}
        {{> meta}}
        <link rel="stylesheet" type="text/scss" href="{{asset "sass/themes.scss"}}" />

    </head>
    <body class="{{body_class}}">
        {{> header}}

        <main class="animated">
            <section class="content">
                {{{body}}}
                {{> footer}}
            </section>
        </main>
        {{> tags-overlay}}
        {{ghost_foot}}

    </body>
</html>
